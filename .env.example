# Proto Trainer Next - Environment Variables

# Development ports (avoid conflicts with other projects)
PORT=3003                    # Next.js dev server
WS_PORT=3004                 # WebSocket relay server
NEXT_PUBLIC_WS_URL=ws://localhost:3004

# Database (SQLite for development)
DATABASE_URL="file:./dev.db"

# OpenAI API
OPENAI_API_KEY=sk-...

# Realtime API (voice training)
REALTIME_MODEL=gpt-4o-realtime-preview
REALTIME_VOICE=shimmer
# Prompt file (in prompts/ directory) - swap for A/B testing
REALTIME_CALLER_PROMPT_FILE=realtime-caller.txt

# Chat API (text training)
CHAT_MODEL=gpt-4.1
# Prompt file (in prompts/ directory) - swap for A/B testing
CHEXT_SIMULATOR_PROMPT_FILE=chext-simulator.txt

# Evaluator API
EVALUATOR_MODEL=gpt-4.1
# Prompt file (in prompts/ directory) - swap for A/B testing
EVALUATOR_PROMPT_FILE=evaluator-v1.txt

# Optional: Vector store for policy file_search
ACCOUNT_POLICIES_VECTOR_STORE_ID=vs_...

# External API (for service-to-service integration with Personalized Training Guide)
EXTERNAL_API_KEY=your-secret-api-key-here
